<UserControl x:Class="cvgPrograma.Views.AgendaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModel="clr-namespace:cvgPrograma.ViewModels"
             xmlns:local="clr-namespace:cvgPrograma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="13"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="{DynamicResource MaterialDesignPaper}"
             FontFamily="{DynamicResource MaterialDesignFont}"
             >
    <UserControl.Resources>
        <viewModel:AgendaViewModel x:Key="vmServico"/>
    </UserControl.Resources>
    <Grid DataContext="{StaticResource vmServico}">
        
    <Grid>
        <!--Novo Serviço-->

            <StackPanel Panel.ZIndex="10" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Button         IsEnabled="True"       
                x:Name="mdDialog"  Margin="10 30"              
    Width="32"
    Padding="0"
    Command="{Binding JanelaNovo}"
    HorizontalAlignment="Right" VerticalAlignment="Top">
                    <materialDesign:PackIcon FontSize="60"  Kind="Plus"/>                    
                </Button>

                <Button Command="{Binding AtualizarCollection}"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="10 30"
                   
                Content="Atualizar"
                Width="128"
        />
            </StackPanel>

            <Grid>
            <Calendar Height="370" Margin="120,70,0,0" HorizontalAlignment="Left" Background="LightCyan"
            Name="calendar1" VerticalAlignment="Top" Width="295" Language="pt-BR"/>
        </Grid>

        <!--<materialDesign:Card HorizontalAlignment="Left"
  Padding="10,5"                           
  Background="#03a9f4"
  Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}" Margin="1000,130,0,240" Width="355" Height="80">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition
        Height="Auto" />
                    <RowDefinition
        Height="Auto" />
                    <RowDefinition
        Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock
      Grid.Row="1"
      Margin="5,4,16,40"
      Style="{StaticResource MaterialDesignHeadline5TextBlock}"
      Text="Cliente" Grid.RowSpan="2" />
                <Separator
      Grid.Row="1"
      Style="{StaticResource MaterialDesignLightSeparator}" />
                <TextBlock
      Grid.Row="2"
      Margin="5,0,0,0"
      HorizontalAlignment="Left"
      VerticalAlignment="Center"
      Style="{StaticResource MaterialDesignBody2TextBlock}"
      Text="Manutenção" Height="20" Width="80" />
                <StackPanel
      Grid.Row="2"
      Margin="16,10,10,8"
      HorizontalAlignment="Right"
      Orientation="Horizontal">
                    <Button
        Width="33"
        Padding="8,0,8,0"
        materialDesign:RippleAssist.IsCentered="True"
        Style="{StaticResource MaterialDesignToolForegroundButton}" RenderTransformOrigin="1.2,0.604" Height="29">
                        <materialDesign:PackIcon
          Kind="Phone" />
                    </Button>

                </StackPanel>
            </Grid>
        </materialDesign:Card>


        <materialDesign:Card HorizontalAlignment="Right"
Padding="10,5"                           
Background="DarkBlue"
Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}" Margin="0,130,550,240" Width="360" Height="80">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition
      Height="Auto" />
                    <RowDefinition
      Height="Auto" />
                    <RowDefinition
      Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock
    Grid.Row="1"
    Margin="5,4,16,40"
    Style="{StaticResource MaterialDesignHeadline5TextBlock}"
    Text="Cliente" Grid.RowSpan="2" />
                <Separator
    Grid.Row="1"
    Style="{StaticResource MaterialDesignLightSeparator}" />
                <TextBlock
    Grid.Row="2"
    Margin="5,0,0,0"
    HorizontalAlignment="Left"
    VerticalAlignment="Center"
    Style="{StaticResource MaterialDesignBody2TextBlock}"
    Text="Manutenção" Height="20" Width="80" />
                <StackPanel
    Grid.Row="2"
    Margin="16,10,10,8"
    HorizontalAlignment="Right"
    Orientation="Horizontal">
                    <Button
      Width="33"
      Padding="8,0,8,0"
      materialDesign:RippleAssist.IsCentered="True"
      Style="{StaticResource MaterialDesignToolForegroundButton}" RenderTransformOrigin="1.2,0.604" Height="29">
                        <materialDesign:PackIcon
        Kind="Phone" />
                    </Button>

                </StackPanel>
            </Grid>
        </materialDesign:Card>


        <materialDesign:Card HorizontalAlignment="Right"
Padding="10,5"                           
Background="BlueViolet"
Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}" Margin="0,200,545,40" Width="370" Height="80">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition
      Height="Auto" />
                    <RowDefinition
      Height="Auto" />
                    <RowDefinition
      Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock
    Grid.Row="1"
    Margin="5,4,16,40"
    Style="{StaticResource MaterialDesignHeadline5TextBlock}"
    Text="Cliente" Grid.RowSpan="2" />
                <Separator
    Grid.Row="1"
    Style="{StaticResource MaterialDesignLightSeparator}" />
                <TextBlock
    Grid.Row="2"
    Margin="5,0,0,0"
    HorizontalAlignment="Left"
    VerticalAlignment="Center"
    Style="{StaticResource MaterialDesignBody2TextBlock}"
    Text="Manutenção" Height="20" Width="80" />
                <StackPanel
    Grid.Row="2"
    Margin="16,10,10,8"
    HorizontalAlignment="Right"
    Orientation="Horizontal">
                    <Button
      Width="33"
      Padding="8,0,8,0"
      materialDesign:RippleAssist.IsCentered="True"
      Style="{StaticResource MaterialDesignToolForegroundButton}" RenderTransformOrigin="1.2,0.604" Height="29">
                        <materialDesign:PackIcon
        Kind="Phone" />
                    </Button>

                </StackPanel>
            </Grid>
        </materialDesign:Card>




        <materialDesign:Card HorizontalAlignment="Left"
Padding="10,5"                           
Background="Black"
Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}" Margin="1000,400,0,240" Width="355" Height="80">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition
      Height="Auto" />
                    <RowDefinition
      Height="Auto" />
                    <RowDefinition
      Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock
    Grid.Row="1"
    Margin="5,4,16,40"
    Style="{StaticResource MaterialDesignHeadline5TextBlock}"
    Text="Cliente" Grid.RowSpan="2" />
                <Separator
    Grid.Row="1"
    Style="{StaticResource MaterialDesignLightSeparator}" />
                <TextBlock
    Grid.Row="2"
    Margin="5,0,0,0"
    HorizontalAlignment="Left"
    VerticalAlignment="Center"
    Style="{StaticResource MaterialDesignBody2TextBlock}"
    Text="Manutenção" Height="20" Width="80" />
                <StackPanel
    Grid.Row="2"
    Margin="16,10,10,8"
    HorizontalAlignment="Right"
    Orientation="Horizontal">
                    <Button
      Width="33"
      Padding="8,0,8,0"
      materialDesign:RippleAssist.IsCentered="True"
      Style="{StaticResource MaterialDesignToolForegroundButton}" RenderTransformOrigin="1.2,0.604" Height="29">
                        <materialDesign:PackIcon
        Kind="Phone" />
                    </Button>

                </StackPanel>
            </Grid>
        </materialDesign:Card> -->

            <ScrollViewer VerticalScrollBarVisibility="Auto" Grid.Row="1" Grid.ColumnSpan="3">
                <ItemsControl ItemsSource="{Binding Servico}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" Margin="20, 5, 20, 5"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="10">
                                <StackPanel Background="Purple" Width="165" Height="200"
                                  VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
                                    <TextBlock Text="{Binding NomeCliente}" FontSize="18" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding TelefoneCliente}" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding CodMetodo}" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding TipoPagamento}" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding CodServico}" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding TipoServ}" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding DescServico}" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding DataEntradaServico}" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding DataSaidaServico}" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding ProntoServico}" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding TotalServico}" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding ClienteId}" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding CodMetodo}" FontSize="14" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding CodServico}" FontSize="14" HorizontalAlignment="Center"/>
                                    <Button x:Name="btnEditarCard" Content="Editar" Height="28" Padding="4"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

    </Grid>
</UserControl>